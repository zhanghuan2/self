{{#component "nav-category-new navbar navbar-condensed evees-comp js-comp"}}
  <div class="hide">{{json this}}</div>
  <div class="navbar-collapse" data-href="{{_HREF_.main}}">
    <ul>
      <li class="home-channel-container {{#if _IS_FIRST_SCREEN_}}home-catgeory-show{{/if}}">
        <div class="navbar-desc eevee-theme-bg">
          <a class="all-category-show" href="javascript:void(0);">
            <i class="icon-zcy icon-quanbushangpinleimu"></i>
            <strong>{{#if _EEVEES_.title}}{{_EEVEES_.title}}{{else}}全部商品分类{{/if}}</strong>
          </a>
        </div>
        <div class="home-channel js-category-list {{#equals _EEVEES_.ifshow 0}}disappear{{/equals}}" style="left: 0">
          <div class="categorys">
            {{#each _DATA_}}
              <div class="category-li " data-id="{{id}}">
                <div class="category-logo">
                  {{#if logo}}
                    <i class="category-icon icon-zcy icon-{{logo}} eevee-theme-icon"></i>
                  {{else}}
                    <img src="http://showcase.b0.upaiyun.com/2015/6/29/7f67297258d4ff2d6bf2db074738bf64.png" alt="" class="category-icon" >
                  {{/if}}&nbsp;
                </div>
                <div class="parent-category">
                  <a href="{{_HREF_.main}}/eevees/search?fcid={{id}}" target="_blank" class="child-category">{{name}}</a>
                </div>
                {{#if children}}
                <div class="expand-panel disappear">
                  <div class="attach"></div>
                  <div class="expand-category">
                    <div class="category-info">
                      {{#each children}}
                        <div class="second-category {{#if @last}}last{{/if}}">
                          <div class="parent-name">
                            <a href="{{_HREF_.main}}/eevees/search?fcid={{current.id}}" target="_blank" class="second-name">{{current.name}}</a>
                          </div>
                          <div class="children-name">
                            {{#each children}}
                              <a class="child-name" href="{{_HREF_.main}}/eevees/search?fcid={{current.id}}" target="_blank">{{current.name}}</a>
                            {{else}}
                              <a class="child-name child-empty" href="javascript:;">&nbsp;</a>
                            {{/each}}
                          </div>
                        </div>
                      {{/each}}
                    </div>
                  </div>
                </div>
                  {{/if}}
              </div>
            {{/each}}
          </div>
        </div>
      </li>
    </ul>
    <div class="navbar-icon">
      <div class="icon-400-tel"></div>
    </div>
  </div>
{{/component}}
